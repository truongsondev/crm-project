import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'layout-auth-component',
  templateUrl: './layout-auth.component.html',
  standalone: true,
  imports: [RouterModule],
})
export class LayoutAuthComponent {}
